# Copy this file to `.env` and fill in real values. `.env` is gitignored.
# Load it in code with peatfire.load_env() (see src/peatfire/data_loading.py).

# NASA FIRMS API key — get one (free, uses your Earthdata login) at:
# https://firms.modaps.eosdis.nasa.gov/api/
FIRMS_MAP_KEY=

# Add more keys below as needed, one per line:
# EARTHDATA_TOKEN=
